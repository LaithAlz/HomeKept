package com.homekept.catalog;

/** Service category — maps to the {@code category} CHECK constraint in V2__catalog.sql. */
public enum ServiceCategory {
    HVAC,
    PLUMBING,
    EXTERIOR,
    SMART_HOME
}
